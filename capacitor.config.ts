import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stencil-csp-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
